
créer un script qui fait du processing csv pour l'import de DATA dans les tables de la BDD -> OK
un script qui permet d'importer un dump de la BDD -> OK
Connaître le rôle de chaques tables de la BDD
savoir comment avoir des modules pré-installé dans le docker compose ou autre
créer un script qui fait un dump de la BDD -> OK
créer un script qui ajoute dans la crontab le script d'export dump de la BDD -> OK
dans le docker compose -> ajout de nom pour les conteneures -> OK
sur le dockerfile de mariadb, faire un script qui install la crontab + import des script à l'intérieure de celle-ci -> OK
